// Objective behaviors QuestManager supports. Designers pick one per QuestObjectiveDefinition;
// adding a new quest with an existing type never requires touching QuestManager.
public enum QuestObjectiveType
{
    Talk,
    Obtain,
    Craft,
    Purchase,
    Gather,
    Kill
}
